<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="appButton" parent="Widget.AppCompat.Button.Small">
        <item name="android:layout_width">318dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_column">1</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:background">#f8854b</item>
        <item name="android:padding">@dimen/abc_button_inset_horizontal_material</item>
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Caption</item>
        <item name="android:textSize">20dp</item>
        <item name="android:clickable">true</item>
        <item name="android:onClick">showToast</item>
    </style>

</resources>
